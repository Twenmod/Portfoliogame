{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/Spelunker.out",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}