#ifndef MATH_H
#define MATH_H

namespace math {
    /**
    * @brief Returns absolute difference between two floats even if they are negative
    * @param a first value
    * @param b second value
    * @return the absolute difference between a and b
    */
    float difference(float a, float b);
}

#endif